import React from "react";

const InputBox = ({ msg }) => {
  return (
    <div>
      <label>{msg}</label>
      <input type="number" name="" id="" />
      <select name="units" id=""></select>
    </div>
  );
};

export default InputBox;
